# Demo build — dummy mock values only. The mock Supabase client
# (src/lib/supabase.ts) ignores these; they exist so URL-builder services
# don't throw "not configured". Safe to commit. Never point at a real project.
VITE_SUPABASE_URL=http://mock.local
VITE_SUPABASE_ANON_KEY=mock-anon-key
